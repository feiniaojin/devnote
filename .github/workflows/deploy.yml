name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v3
        with:
          node-version: 16
          cache: yarn
      - run: yarn install --frozen-lockfile

      - name: Build
        run: yarn docs:build

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: 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
          publish_dir: docs/.vitepress/dist
          # cname: example.com # if wanna deploy to custom domain